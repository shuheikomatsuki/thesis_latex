\documentclass[twocolumn, a4paper]{UECIEresume}
\usepackage[dvipdfmx]{graphicx}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{txfonts}
\usepackage{url}

\title{卒論予稿フォーマット}
\date{2026 年 mm 月 dd 日}
\affiliation{I類 メディア情報学 プログラム}
%\affiliation{総合情報学科 XXX コース}
%\affiliation{情報学専攻 XXX プログラム}
\supervisor{高木一幸 助教}
\studentid{2311093}
\author{駒月柊平}
\headtitle{2026 年度 I類 卒研中間発表}
%\headtitle{20yy 年度 総合情報学科 卒研中間発表}
%\headtitle{20yy 年度 I類 卒研発表}
%\headtitle{20yy 年度 総合情報学科 卒研発表}
%\headtitle{20yy 年度 情報学専攻 修士論文中間発表}
%\headtitle{20yy 年度 情報学専攻 修士論文発表}

\begin{document}
\maketitle

\section{はじめに}

% 「はじめに」の部分はモデルの導入部，背景などを中心に自分の研究の位置づけを書いて下さい．
% 必要であれば，引用文献を使って自分の立ち位置を記述することが重要です\cite{Kinoshita}．
% 研究内容をいきなりモデルや実験の説明から始めてはいけません．
% 研究目的を達成するために，どのようなアプローチをとるのか，
% これから述べる方法で，何が何処まで明らかに(あるいは期待)出来るかなどを書く必要があるでしょう．

近年，短いプロンプト音声を用いて，学習時に含まれない話者の音声を
追加学習なしで合成するzero-shot TTSが研究されている
\cite{Wang2023VALLE}．
さらに，zero-shot TTSを多言語に拡張し，プロンプト音声とは異なる言語でも
話者性を保持した音声を生成するcross-lingual TTSが提案されている
\cite{Zhang2023VALLEX}．
これにより，対象話者が実際には話していない言語についても，
その話者らしい音声を合成することが期待される．

一方，多言語zero-shot TTSでは，学習に用いられた言語や音声量の違いにより，
生成言語ごとに発話内容の正確性，自然性，および話者性の保持性能が
異なる可能性がある．


（TODO: ここに追加で何か書くかも。どう問題があったのか。予備実験とか。）


% 特に日本語は，英語とは音韻体系，アクセント体系，
% および表記体系が異なるため，既存の多言語モデルにおける日本語合成性能を
% 明らかにし，その改善方法を検討する必要がある．また，cross-lingual合成では，
% 対象言語として自然な音声を生成するだけでなく，異なる言語のプロンプト音声が
% 持つ話者性を保持することが求められる．

そこで本研究では，多言語zero-shot TTSにおける日本語を対象言語とし，
プロンプト音声の言語によらず，発話内容が正確で，自然かつ対象話者らしい
日本語音声を合成することを目的とする．既存の多言語TTSモデルに対して
日本語音声データを用いた追加学習を行い，追加学習前後のモデルを比較する．
さらに，日本語への適応によって既存の他言語合成能力が低下しないかを確認する．

% TODO: 「既存モデルでは日本語品質が低い」と断定するには，予備評価結果または
% 先行研究による根拠が必要である．基盤モデル決定後，日本語->日本語，
% 英語->日本語，英語->英語，日本語->英語の予備生成結果を踏まえて追記する．
% TODO: 基盤モデルはX-VoiceとCosyVoice 3などを予備評価し，公開された学習コード，
% 日本語対応，計算資源，および追加学習の実現可能性を考慮して決定する．

% 一方，予備的に既存モデルの合成音声を聴取したところ，英語を生成する場合と
% 比較して，日本語を生成する場合には声質が機械的または平均的に感じられ，
% プロンプト音声の話者らしさが十分に保持されない傾向が見られた．
% このことから，cross-lingual TTSでは，生成言語によって話者性の保持性能が
% 異なる可能性がある．

% そこで本研究では，プロンプト音声が持つ話者性を保持しながら，
% 異なる言語，特に日本語の音声を生成できるcross-lingual TTSの実現を目的とする．
% X-VoiceまたはCosyVoice3を基盤モデルの候補とし，日本語を含む音声データを用いた
% 追加学習やLow-Rank Adaptation（LoRA）の適用を検討する．
% 追加学習前後のモデルを比較し，発話内容の正確性，音声の自然性，
% およびプロンプト音声に対する話者類似性の観点から有効性を検証する．

\section{提案手法}

本研究では，既存の多言語zero-shot TTSモデルを基盤とし，日本語テキストと
音声の対を用いてモデルを追加学習する．入力として対象話者の短いプロンプト音声と
日本語テキストを与え，プロンプト音声の話者性を保持した日本語音声を生成する．

追加学習では，基盤モデルが持つzero-shot話者生成能力を維持しながら，
日本語の発音および韻律に関係する部分を適応する．

（TODO: 具体的なやり方は後で書く。要検討。）

% 具体的には，プロンプト音声から話者情報を抽出する部分を固定し，テキストから音声表現を生成する部分を主な
% 更新対象とする．
% 全パラメータを更新する追加学習と，Low-Rank Adaptation
% （LoRA）によって一部のパラメータのみを更新する方法を候補とし，日本語の
% 合成品質，話者類似性，および他言語合成能力の変化を比較する．


比較対象には追加学習前の基盤モデルを用いる．同一のプロンプト音声および
入力テキストから追加学習前後の音声を生成し，（後で書く）% 日本語プロンプトから日本語を
% 生成する同一言語条件と，英語プロンプトから日本語を生成するcross-lingual条件を
% 評価する．また，日本語への追加学習による既存性能の低下を確認するため，
% 英語音声を生成する条件についても補助的に評価する．

% TODO: 基盤モデル決定後，モデル構成に合わせて「話者情報を抽出する部分」と
% 「テキストから音声表現を生成する部分」の具体的なモジュール名を記載する．
% TODO: 全パラメータの追加学習とLoRAの両方を実施できるかは計算資源を確認して
% 決定する．LoRAのみを実施する場合は，その選択理由と挿入箇所，rank，alpha，
% dropoutなどの設定を記載する．
% TODO: 「話者関連部分を固定する」という方針は，基盤モデルの構造と予備実験に
% 基づいて確定する．固定した場合に日本語音韻への適応を妨げないかも確認する．
% TODO: 最終的には，学習率，バッチサイズ，更新回数，最適化手法，使用GPU，
% 学習時間，チェックポイントの選択基準を記載する．

\section{データセット}

日本語への追加学習には，複数話者による日本語音声と発話テキストの対を用いる．
特定の学習話者へ声質が偏ることを避けるため，単一話者データのみではなく，
多話者データを中心に構成する．音声は基盤モデルの入力条件に合わせて
サンプリング周波数を統一し，発話前後の過度な無音を除去する．また，数字，
英字，記号などの表記揺れを抑えるため，発話テキストに正規化処理を施す．

（TODO: 具体的などういうデータセットを使うかは後で書く。要検討。ATR MANYも候補。）

追加学習用，検証用，および評価用のデータは分離する．特にzero-shot話者生成を
評価するため，評価に用いる話者は追加学習用データに含まれる話者と重複しない
ようにする．評価用データでは，日本語および英語のプロンプト音声と，
% 日本語を中心とする合成用テキストを用意する．各手法には同一のプロンプト音声と
% 入力テキストを与え，条件間で発話内容や話者が偏らないようにする．

% TODO: 日本語追加学習用データセットは，JVSなどの多話者コーパスを第一候補とし，
% 利用条件，音声時間，話者数，録音品質を確認して決定する．必要に応じてJSUT，
% Common Voice Japanese，ReazonSpeechなどの併用を検討する．
% TODO: データセット決定後，名称，総発話数，総時間，話者数，男女比，
% サンプリング周波数，ライセンス，および学習・検証・評価の分割数を記載する．
% TODO: 評価用の未見話者とプロンプト音声の入手元を決定する．英語話者による
% 英語プロンプトから日本語を生成する条件を，主要なcross-lingual条件とする．
% TODO: テキスト正規化，音量正規化，無音区間除去，短すぎる音声や雑音を含む
% 音声の除外基準を，実際に用いる前処理に合わせて記載する．

\section{評価方法}

提案手法の有効性を検証するため，発話内容の正確性，音声の自然性，
およびプロンプト音声に対する話者類似性の3つの観点から評価を行う．
比較対象には追加学習前のモデルを用い，同一のプロンプト音声と
入力テキストから生成した音声を評価する．

\subsection{客観評価}

発話内容の正確性は，合成音声を多言語音声認識モデルWhisper
\cite{Radford2023Whisper}のlarge-v3\cite{OpenAI2023WhisperLargeV3}によって文字起こしし，
入力テキストを正解文として評価する．英語音声には単語誤り率
（Word Error Rate: WER），日本語音声には文字誤り率
（Character Error Rate: CER）を用いる．WERおよびCERは，置換数を
$N_{\mathrm{S}}$，削除数を$N_{\mathrm{D}}$，挿入数を$N_{\mathrm{I}}$，
正解文の単語数または文字数を$N$として，次式により算出する．
\begin{equation}
  \mathrm{Error\ Rate}
  = \frac{N_{\mathrm{S}}+N_{\mathrm{D}}+N_{\mathrm{I}}}{N}.
\end{equation}
値が小さいほど，入力テキストの内容が正確に発話されていることを示す．

話者類似性の評価にはSpeaker Similarity（SIM）を用いる．
プロンプト音声と合成音声から話者埋め込み
$\boldsymbol{e}_{\mathrm{p}}$および$\boldsymbol{e}_{\mathrm{s}}$を抽出し，
両者のコサイン類似度を求める.
% 両者のコサイン類似度を次式により求める．
% \begin{equation}
%   \mathrm{SIM}
%   = \frac{\boldsymbol{e}_{\mathrm{p}}^{\mathsf{T}}
%   \boldsymbol{e}_{\mathrm{s}}}
%   {\lVert\boldsymbol{e}_{\mathrm{p}}\rVert_2
%   \lVert\boldsymbol{e}_{\mathrm{s}}\rVert_2}.
% \end{equation}
値が大きいほど，合成音声にプロンプト話者の特徴が保持されていることを
示す．また，合成音声の自然性および音質を自動的に推定する指標として
UTMOS\cite{Saeki2022UTMOS}を用いる．

\subsection{主観評価}

客観指標のみでは知覚的な自然性や話者らしさを十分に評価できないため，
聴取実験による主観評価を行う．音声の自然性はMean Opinion Score
（MOS）を用い，「非常に不自然」から「非常に自然」までの5段階で
評価する．話者類似性はSpeaker Mean Opinion Score（SMOS）を用い，
プロンプト音声と合成音声を提示して，「全く似ていない」から
「非常に似ている」までの5段階で評価する．

さらに，追加学習前後の音声を直接比較する場合にはComparative Mean
Opinion Score（CMOS）を用いる．同一条件で生成した2つの音声を
提示順序を無作為化して提示し，提案手法の音声が比較対象よりどの程度
優れているかを$-3$から$+3$の7段階で評価する．MOSおよびUTMOSにより
自然性，SIMおよびSMOSにより話者類似性，WERまたはCERにより発話内容の
正確性をそれぞれ評価し，これらを総合して提案手法の有効性を検証する．


\section{おわりに}

% 最後は簡潔に研究成果をまとめて下さい．将来の課題などもあれば書いても良いですが，あまり課題を書きすぎると逆効果になりますのでほどほどにしておきましょう．

% また，引用文献はキチンと入れましょう\cite{Kinoshita}．引用は，先人に対するリスペクトなので，よほど独立性が高い研究でない限り必要となります．

本研究では，cross-lingual TTSにおいて，合成音声の言語が異なる場合にも
プロンプト音声の話者性を保持することを目的とする．特に日本語を対象言語とし，
既存の多言語TTSモデルへの追加学習やLoRAの適用により，発話内容の正確性と
話者性を維持しながら，日本語合成音声の自然性を改善する方法を検討する．

今後は，基盤モデルと日本語音声データを決定し，既存モデルの言語および
プロンプト言語ごとの性能を予備評価する．その結果に基づいて更新対象を決定し，
追加学習前後のモデルを客観評価および聴取実験により比較する．

% TODO: 最終発表時には，ここを実験結果と得られた知見の要約に置き換える．

% 今後は，X-VoiceおよびCosyVoice3の予備評価を行い，話者類似性，
% 日本語音声の自然性，追加学習の実現可能性を踏まえて基盤モデルを決定する．
% その後，使用するデータセットと学習条件を整備し，追加学習前後のモデルから
% 同一条件で音声を生成する．WERまたはCER，SIM，UTMOSによる客観評価と，
% MOS，SMOS，CMOSによる主観評価を行い，発話内容の正確性を維持しながら，
% 日本語音声の自然性および話者類似性を改善できるかを検証する．

{\small
\bibliographystyle{junsrt}
\bibliography{template}
}
\end{document}
